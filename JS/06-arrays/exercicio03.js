//3) Faça um programa que dado uma lista de nomes imprima os nomes que começem com a letra "V".

const letras = ["Thalita", 'Victoria','Ana', 'Victor', 'Valeria'];


for ( let i = 0 ; i < letras.length ; i++) {
    const letra = letras [i];
    if (numero % 2 === 0){
        console.log(numero);
    }
}